<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Express Retake Exam January 2019</title>
    <link href="../static/css/style.css" type="text/css" rel="stylesheet">

</head>

<body>

        <%- include("partials/header"); -%>

        <% if (errorMessages.length > 0) { %>
            <%- include("./partials/notifications"); %>
        <% } %>


        <main>
            <form class="theater-form" action="/edit/<%= _id %>" method="POST">
                <h1>Edit Theater</h1>
                <div>
                    <label for="title">Theater Title:</label>
                    <input type="text" placeholder="Theater name" value="<%= title %>" name="title">
                </div>
                <div>
                    <label for="description">Theater Description:</label>
                    <textarea placeholder="Description" name="description"><%= description %></textarea>
                </div>
                <div>
                    <label for="imageUrl">Image url:</label>
                    <input type="text" placeholder="Image Url" value="<%= imageUrl %>" name="imageUrl">
                </div>
                <div class="check">
                    <input type="checkbox" id="check-box" name="isPublic">
                    <label for="check-box">Public</label>
                </div>
                <button class="btn" type="submit">Submit</button>
            </form>
        </main>

        <%- include("partials/header"); -%>

</body>

</html>